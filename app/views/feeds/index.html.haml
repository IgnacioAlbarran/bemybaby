.row
  %h3.col-6 Registro de hoy
  = link_to 'Nueva toma', new_feed_path(session[:baby_id]), class: 'btn.btn-success.btn-sm'
%p
.container
  .row
    .col-sm
      = render 'table'
    .col-sm
      = render 'table_week'
%p
  = "El total de tomas en el dia:  #{@total_feeds} ml - La media diaria de la semana es:  #{@week_average} ml "
%p
  = "Toma media a la edad de #{@baby.name} es de #{@baby.min_feed}"
.container
  .row
    .col-sm
      = form_with(url:feeds_path, method: :get, local: true) do |f|
        .field{:style => "color: green" }
          = f.label :date, 'Seleccionar otra fecha:'
          %br
          = f.text_field :date, id: :datepicker, value: @day.strftime("%d/%m/%Y")
        .actions
          %br
          = f.submit 'Buscar'
    .col-sm
      = line_chart @data.group_by_day(:date).sum(:mililitres), width: "400px", height: "200"

:javascript
  $('#datepicker').datepicker({format: 'dd/mm/yyyy'})
-# %p
-#   = "El total de tomas en el dia:  #{@total_feeds} ml - La media diaria de la semana es:  #{@week_average} ml "
-# %p
-#   %button.btn.btn-success.btn-sm
-#     = link_to 'Nueva toma', new_feed_path(session[:baby_id])
-# %p
-# = form_with url:feeds_path, method: :get do |f|
-#   .field{:style => "color: green" }
-#     = f.label :date, 'Seleccionar otra fecha:'
-#     %br
-#     = f.date_field :date, id: :datepicker
-#   .actions
-#     %br
-# -#     = f.submit 'Buscar'

-# = line_chart @data.group_by_day(:date).sum(:mililitres), width: "400px", height: "200"

